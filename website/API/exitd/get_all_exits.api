FILE
	/usr/common/sys/exitd.c

NAME
	get_all_exits - get all valid exit numbers

PROTOTYPE
	int *get_all_exits(void)

CALLED BY
	System, Common, Game

DESCRIPTION
	Return an array of integers.  Each integer is an exit number
	for a valid exit object.

RETURN VALUE
	An array of exit numbers, or nil.

ERRORS
	Normally none.

SEE ALSO
	get_exit_segments, get_exit_by_num, exits_in_segment
